package tetris.engine;

/**
 * Input commands consumed by GameState.
 */
public enum GameAction {
    START,
    LEFT,
    RIGHT,
    DOWN,
    HARD_DROP,
    ROTATE,
    HOLD,
    PAUSE,
    RESUME,
    RESTART,
    BACK_TO_MENU
}
